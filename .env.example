# Last.fm API key for the "now playing" widget (read-only, public-grade).
# Create one at https://www.last.fm/api/account/create — only the API key is needed
# (the shared secret is for signed/write requests and is NOT used here).
# Copy this file to .env.local and fill in the value.
LASTFM_API_KEY=
